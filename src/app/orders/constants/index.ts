import { ANY_SYMBOL } from "src/app/shared/constants";

export const CONFIRM_ORDER = new RegExp(`CONFIRM_ORDER_${ANY_SYMBOL}`);
export const CONFIRM_PAYMNET = new RegExp(`CONFIRM_PAYMNET_${ANY_SYMBOL}`);
